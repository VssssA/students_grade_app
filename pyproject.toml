[tool.ruff]
line-length = 100
target-version = "py311"

select = [
    "E",   # pycodestyle
    "F",   # ошибки
    "I",   # сортировка импортов
    "UP",  # modern python
]

fix = true


[tool.mypy]
plugins = ["pydantic.mypy"]
python_version = "3.11"
strict = true